{
  "private": true,
  "name": "meeplitis-config",
  "version": "1.0.0",
  "description": "Gets the public settings for a Supabase client connection",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Mario T. Lanza",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}

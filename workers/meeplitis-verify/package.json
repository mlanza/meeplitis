{
  "private": true,
  "name": "meeplitis-verify",
  "version": "1.0.0",
  "description": "Verifies the access token for a user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Mario T. Lanza",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
